import base64
#abrindo arquivo
arquivo = open('C:\GR\GRPdv\GRPdvSync\GR.Snc.Client.Wpf.exe.config', 'r')

print(arquivo.read())
# #decoder
# Codigo_Base64 = arquivo
# base64_bytes = Codigo_Base64.encode('ascii')
# message_bytes = base64.b64decode(base64_bytes)
# decodificado = message_bytes.decode('ascii')

# print(decodificado)
# print(len(decodificado))cle
